# database url, here is the supported databases list: https://tortoise-orm.readthedocs.io/en/latest/databases.html
database_url = "sqlite://linksdb.sqlite"
# the port to run the app
port = int("8000")
